// Optional chaining.
// optional chaining sign (?.).
const user= {
    id: 324555,
    address: {household:"Sonia Vihar"}
}

// If there is no key and property and we don't want to get any error
// So we can use optional chaining (?.). 

// console.log(user?.id);
console.log(user);
// console.log(user?.address?.household);



const user1= {
    // id: 324555,
    // address: {household:"Sonia Vihar"}
}
console.log(user1?.fname2);
console.log(user?.fname);
