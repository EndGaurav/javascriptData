// for loop in Array

let language = ["Java", "Javascript", "HTML", "CSS"];
let lang = []; 
for(let i= 0; i<language.length; i++){
    // console.log(language[i].toUpperCase());
    lang.push(language[i].toUpperCase());
}
console.log(lang);
console.log(language);